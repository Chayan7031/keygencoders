/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["flowbite.s3.amazonaws.com", 'images.unsplash.com', "res.cloudinary.com", "pbs.twimg.com"],
  },
};

export default nextConfig;
